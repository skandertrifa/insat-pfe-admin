import { StudentSearchPipe } from './student-search.pipe';

describe('StudentSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
